<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
    <title>Danh sách khóa học</title>
    <style>
        .card {
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        .card:hover {
            transform: scale(1.05);
        }
        .card-img-top {
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
            height: 180px;
            object-fit: cover;
        }
        .card-title {
            font-size: 1.2rem;
            font-weight: bold;
            color: #000; /* Màu đen cho tiêu đề */
            margin-top: 10px;
            margin-bottom: 10px;
        }
        .card-text {
            font-size: 0.9rem;
            color: #555;
            height: 50px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }
        .btn-primary {
            width: 100%;
            font-size: 0.9rem;
            padding: 8px;
            border-radius: 5px;
            font-weight: bold;
            margin-top: 10px;
            background-color: #d3d3d3; /* Màu xám cho nền nút */
            color: #000; /* Màu đen cho chữ trong nút */
            border: none;
        }
        .btn-primary:hover {
            background-color: #bfbfbf; /* Màu xám đậm hơn khi hover */
        }
    </style>
</head>
<body>

<div class="container mt-4">
    <div class="row">
        {{#each courses}}
            <div class="col-sm-6 col-lg-4 mt-4">
                <div class="card" style="width: 18rem;">
                    <a href="/courses/{{this.slug}}">
                        <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
                    </a>
                    <div class="card-body text-center">
                        <a href="/courses/{{this.slug}}">
                            <h5 class="card-title">{{this.name}}</h5>
                        </a>
                        <p class="card-text">{{this.description}}</p>
                        <a href="/courses/{{this.slug}}" class="btn btn-primary">Bắt đầu học</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>

</body>
</html>